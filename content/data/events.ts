/*
 * Events content (design.md C6). PLACEHOLDER STATE — EMPTY.
 * No real events supplied; we do not invent titles, dates, locations or
 * registration links. Upcoming/past is derived from `date` when real events
 * exist; empty upcoming → "No events currently scheduled." (design.md C6).
 */
import type { LawsanEvent } from "../types";

export const events: LawsanEvent[] = [];
