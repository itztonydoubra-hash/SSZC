/*
 * Administration Archive content (design.md C9). PLACEHOLDER STATE — EMPTY.
 * Past sessions, directors, teams, achievements and photos must come from the
 * client. We do NOT invent history. Incomplete records, when supplied, render
 * only their confirmed fields (design.md C9).
 */
import type { Administration } from "../types";

export const administrations: Administration[] = [];
