/*
 * Chapters content (design.md B2). PLACEHOLDER STATE.
 *
 * The official list of South South states and their recognised chapters has NOT
 * been supplied, so `states` is EMPTY. We do NOT invent states, universities,
 * counts, or node positions (design.md B2: "unsupplied states show the node with
 * 'chapters to be confirmed', never fabricated dots"). When the authoritative
 * list arrives, add each state with a DESIGNED (stylised, non-geographic)
 * layout position and its officially recognised chapters.
 *
 * zoneLabel is the fixed root of the org network; it is a structural label
 * (not organisational content), naming this zone.
 */
import type { Chapters } from "../types";

export const chapters: Chapters = {
  zoneLabel: "South South Zone",
  // Officially recognised chapters not yet supplied — empty (no fabricated nodes).
  states: [],
};
