/*
 * Partners content (design.md C11). OPTIONAL — EMPTY.
 * No approved partners supplied; the Partners section is OMITTED ENTIRELY when
 * this array is empty (design.md C11). We do not invent partner names/logos.
 */
import type { Partner } from "../types";

export const partners: Partner[] = [];
