/*
 * Projects & Initiatives content (design.md C5). PLACEHOLDER STATE — EMPTY.
 * No real projects supplied; we do not invent project names, dates, locations,
 * beneficiaries, results or imagery. Add real Project records when supplied.
 */
import type { Project } from "../types";

export const projects: Project[] = [];
